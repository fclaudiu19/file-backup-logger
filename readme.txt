Backup Tool

Features
- Backup folders with timestamp and versioning
- Optional ZIP compression
- Logs all operations to `backup.log`
- GUI for folder selection and control
- Configuration saved to `config.json`
How to Run

Make sure you have Python 3 installed.
Run the program:

```bash
python main.py